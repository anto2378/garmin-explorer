[project]
name = "garmin-explorer"
version = "0.1.0"
description = "Shared Garmin Connect dashboard for friends — built with Streamlit"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = ["streamlit>=1.41.0", "garminconnect>=0.2.38", "plotly>=6.0.0"]
